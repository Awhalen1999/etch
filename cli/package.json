{
  "name": "@etch/cli",
  "version": "0.0.1",
  "description": "etch — a text horror game in your terminal.",
  "type": "module",
  "bin": {
    "etch": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/index.tsx",
    "build": "esbuild src/index.tsx --bundle --platform=node --target=node20 --format=esm --jsx=automatic --banner:js=\"#!/usr/bin/env node\" --outfile=dist/index.js"
  },
  "dependencies": {
    "ink": "^5.0.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
