name = "etch-api"
main = "src/index.ts"
compatibility_date = "2025-05-01"

# D1 database binding. Run `pnpm db:create` first, then paste the returned
# database_id below before `pnpm dev` or `pnpm deploy`.
[[d1_databases]]
binding = "DB"
database_name = "etch"
database_id = "7d855191-aed0-4bd3-9a49-6ccab8ad19f4"
