{
  "name": "@etch/api",
  "version": "0.0.1",
  "description": "etch inscription service — Cloudflare Worker + D1.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create etch",
    "db:migrate": "wrangler d1 execute etch --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
