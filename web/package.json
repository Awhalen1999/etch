{
  "name": "@etch/web",
  "version": "0.0.1",
  "description": "etch.rip — the landing page.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
